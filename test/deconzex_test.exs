defmodule DeconzexTest do
  use ExUnit.Case
  doctest Deconzex

  # test "Application will start" do
  #   assert {:ok, _} = Application.ensure_all_started(:deconzex)
  #   assert %{network_state: :net_connected} = Deconzex.Device.device_state
  # end
end
